[project]
name = "sinnix-quota"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
sinnix-quota = "sinnix_quota.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
