[project]
name = "sinnix-ops-reducer"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
sinnix-ops-reducer = "sinnix_ops_reducer.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
