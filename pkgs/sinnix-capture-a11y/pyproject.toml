[project]
name = "sinnix-capture-a11y"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
sinnix-capture-a11y = "sinnix_capture_a11y.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
