[project]
name = "sinnix-audio-capture"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
sinnix-audio-capture = "sinnix_audio_capture.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
