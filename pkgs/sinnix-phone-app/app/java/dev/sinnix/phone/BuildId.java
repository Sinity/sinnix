package dev.sinnix.phone;

/** Build identity, surfaced in status.json so the desktop can tell which APK is live. */
final class BuildId {
  static final String VERSION = "0.1.0";

  private BuildId() {}
}
