--- a/devtools/worker_memory.py
+++ b/devtools/worker_memory.py
@@ -355 +355,2 @@
-    Never zero: a slow run beats a run that does not start.
+    Zero when the cgroup cannot hold the controller plus one worker: a typed
+    deferral beats a one-worker launch that will be killed.
@@ -358,4 +359,3 @@
-    # share.  A negative margin at width 1 still returns one: slow work is
-    # preferable to refusing to start, and the negative admission margin is
-    # retained in the receipt for the owner to act on.
-    workers = 1
+    # share.  A run that cannot hold its controller plus one worker is
+    # deferred by the slot before it starts.
+    workers = 0
@@ -643 +643,2 @@
-    workers = max(1, min(requested, width_within(cgroup)))
+    workers = min(requested, width_within(cgroup))
+    admission = "admitted" if workers >= 1 else "resource_not_ready"
@@ -658,0 +660 @@
+        "admission": admission,
--- a/devtools/pytest_slot.py
+++ b/devtools/pytest_slot.py
@@ -79,0 +80,4 @@
+#: Typed deferral when the cgroup cannot hold controller plus one worker.
+RESOURCE_NOT_READY: Final = "resource_not_ready"
+#: sysexits.h EX_TEMPFAIL: bounded, retryable, not a pytest failure.
+EX_TEMPFAIL: Final = 75
@@ -799 +803 @@
-    if sizing is None or not sizing.get("narrowed"):
+    if sizing is None:
@@ -801,0 +806,8 @@
+    if _admission_not_ready(sizing):
+        return (
+            f"pytest slot: {sizing['available_mib']} MiB from {bound} "
+            f"(host {sizing['host_available_mib']} MiB, cgroup {sizing['cgroup_available_mib']} MiB) "
+            "cannot hold controller plus one worker; deferring."
+        )
+    if not sizing.get("narrowed"):
+        return None
@@ -809,0 +822,10 @@
+def _admission_not_ready(sizing: Mapping[str, Any] | None) -> bool:
+    """True when sizing forbids launching even one worker."""
+    if sizing is None:
+        return False
+    if sizing.get("admission") == RESOURCE_NOT_READY:
+        return True
+    workers = sizing.get("workers")
+    return isinstance(workers, int) and workers < 1
+
+
@@ -939,0 +962,9 @@
+    if _admission_not_ready(sizing):
+        return EX_TEMPFAIL, _slot_receipt(
+            status=RESOURCE_NOT_READY,
+            exit_code=EX_TEMPFAIL,
+            elapsed_s=time.monotonic() - started,
+            sizing=sizing,
+            memory=None,
+            extra={"diagnosis": "pytest_resource_not_ready"},
+        )
@@ -1180,0 +1212,21 @@
+    if _admission_not_ready(sizing):
+        receipt = _slot_receipt(
+            status=RESOURCE_NOT_READY,
+            exit_code=EX_TEMPFAIL,
+            elapsed_s=time.monotonic() - started,
+            sizing=sizing,
+            memory=None,
+            log_path=log_path,
+            extra={"diagnosis": "pytest_resource_not_ready"},
+        )
+        with open(log_path, "wb") as log:
+            if note is not None:
+                log.write((note + "\n").encode())
+                log.flush()
+        with contextlib.suppress(OSError):
+            _persist_slot_result(log_path, receipt)
+        _print_result(receipt)
+        for number, handler in previous.items():
+            with contextlib.suppress(ValueError, OSError):
+                signal.signal(number, handler)
+        return EX_TEMPFAIL
