#!/usr/bin/env bash
# @sinnix-package: skip
set -euo pipefail

if command -v code >/dev/null 2>&1; then
  exec code --reuse-window
fi

printf '%s\n' "open-code-editor: code is not available" >&2
exit 127
