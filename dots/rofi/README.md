# Rofi Configuration

This directory contains configuration for Rofi, an application launcher for Linux.

## Installation

Use the manage-dots.sh script to deploy this configuration:

```bash
./manage-dots.sh deploy rofi
```

## Configuration Files

- `config.rasi`: Main configuration file with behavior settings
- `theme.rasi`: Theme configuration with gruvbox-inspired colors
