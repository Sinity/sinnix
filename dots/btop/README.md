# btop Configuration

This directory contains configuration for btop, a system resource monitor.

## Installation

Use the manage-dots.sh script to deploy this configuration:

```bash
./manage-dots.sh deploy btop
```

## Configuration Files

- `btop.conf`: Main configuration file with theme and update settings
