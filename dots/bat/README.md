# bat Configuration

This directory contains configuration for bat, a cat clone with syntax highlighting.

## Installation

Use the manage-dots.sh script to deploy this configuration:

```bash
./manage-dots.sh deploy bat
```

## Configuration Files

- `config`: Main configuration file with pager and theme settings
